
  # Bread Workshop App Design

  This is a code bundle for Bread Workshop App Design. The original project is available at https://www.figma.com/design/17AytDhjW0RSLNLtecDBom/Bread-Workshop-App-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  